import { Navigation, NavigationOutlined } from "@mui/icons-material";
import React, { useState, useEffect } from "react";

export const Car = (props: {}) => {
  
  return (
    <div>
      <Navigation style={{ fontSize: "20px", color: "green" }} />
    </div>
  );
};
